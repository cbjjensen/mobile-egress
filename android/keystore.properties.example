# Copy this file to keystore.properties. The copy is ignored by Git.
# Keep the keystore outside the repository and use an absolute path.
storeFile=C:\\secure\\mobile-egress-release.jks
storePassword=replace-with-local-secret
keyAlias=mobile-egress
keyPassword=replace-with-local-secret
