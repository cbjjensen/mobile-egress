# Copy this file to keystore.properties. The populated copy is ignored by Git.
# The named JKS is also ignored; reuse and externally back up both files together.
storeFile=mobile-egress-release.jks
storePassword=replace-with-local-secret
keyAlias=mobile-egress
keyPassword=replace-with-local-secret
